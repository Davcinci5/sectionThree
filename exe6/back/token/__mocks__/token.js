const resolveToken = async (token) => {
    return token;
};

const createToken = () => {
    return true;
};

const clearCookie = () => {
    return true;
};

const setToken = () => {
    return true;
};

module.exports = { createToken, resolveToken, clearCookie, setToken };
